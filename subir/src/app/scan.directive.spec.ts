import { ScanDirective } from './scan.directive';

describe('ScanDirective', () => {
  it('should create an instance', () => {
    const directive = new ScanDirective();
    expect(directive).toBeTruthy();
  });
});
